name = "luna-letter"
main = "mail-worker.mjs"
compatibility_date = "2026-09-14"

[vars]
ALLOWED_ORIGIN = "https://kawase-creative.github.io"
# Set MAIL_FROM to an address on your Resend-verified domain before deployment.

[[ratelimits]]
name = "MAIL_LIMITER"
namespace_id = "1001"
simple = { limit = 3, period = 60 }
